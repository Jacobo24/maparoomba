import tkinter as tk
hay que usar canvas

self.convas.tag_bind(...)


class zona (x,y,xob, yob):
    get_area(x,y):
        return float
    get_area_ob(xob,yob):
        return float
    get_zonas(x,y):
        return float


    self.convas.tag_bind(...)


class zona (nombre, area)